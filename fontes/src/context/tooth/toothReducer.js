import {
    SAVE_STATE,
    BUY_ITEM,
    MOUTH_CLICK,
    GET_STATE
} from '../types';

export default (state, action) => {
    switch (action.type) {
        case GET_STATE:
            return {

            };
        case SAVE_STATE:
            return {

            };
        case BUY_ITEM:
            return {

            };
        case MOUTH_CLICK:
            return {

            };
    }
}